# =============================================================================
# WAXAL ASR — Python Dependencies
# =============================================================================
# Install with: pip install -r requirements.txt
# =============================================================================

# Core ML
torch>=2.1.0
transformers>=4.52.0
datasets>=2.18.0
accelerate>=0.30.0

# Parameter-efficient fine-tuning
peft>=0.11.0
trl>=0.12.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
timm>=1.0.0

# Evaluation
jiwer>=3.0.0

# Quantisation (optional, for 4-bit/8-bit loading)
bitsandbytes>=0.43.0
torchao>=0.1.0

# Configuration
pyyaml>=6.0

# Utilities
numpy>=1.24.0
tqdm>=4.65.0
huggingface_hub>=0.23.0

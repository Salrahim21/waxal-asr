# =============================================================================
# WAXAL ASR — Lingala (lin) Language Override
# =============================================================================
# Inherits all defaults from configs/default.yaml.
# Only overrides specific to Lingala training are listed here.
# =============================================================================

dataset:
  languages:
    - "lin"

training:
  output_dir: "./outputs/gemma3n-asr-lin"
  max_steps: 500
